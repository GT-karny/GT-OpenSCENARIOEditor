# Dev API proxy target for the Vite dev server (default: http://localhost:3001).
# The default points at the local @osce/server on DEFAULT_SERVER_PORT. Override
# only to proxy /api at a non-default backend, e.g. a remote host or a server
# running on another port.
# VITE_API_PROXY_TARGET=http://localhost:3001
